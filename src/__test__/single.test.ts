import { textsecure } from '../protos'

describe('New Crypto Tests 2020', function () {
    test(`createKeyPair converts alice's private keys to a keypair`, async () => {
        console.log(textsecure.SignalMessage)
    })
})
